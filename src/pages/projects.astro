---
import Layout from '../layouts/Layout.astro';
const pageTitle = 'Projects';
---

<Layout pageTitle={pageTitle}>
  {/* Headings */}
  <h1>Projects</h1>
  <h2>This is a Subheading</h2>
  <h3>This is a Smaller Subheading</h3>
  <h4>This is a Sub-Subheading</h4>
  <h5>This is a Minor Subheading</h5>
  <h6>This is the Smallest Heading</h6>

  <hr />

  {/* Standard Body Text */}
  <p>
    This is a standard <strong>paragraph</strong> used for bulk text. You can use
    <em>emphasis</em> to highlight points, or <mark>mark</mark> to highlight text
    like a highlighter pen.
  </p>

  <p>
    <small
      >This is small text, often used for legal disclaimers or copyright
      notices.</small
    >
  </p>

  {/* Lists */}
  <ul>
    <li>This is an unordered list item</li>
    <li>It typically uses bullet points</li>
  </ul>

  <ol>
    <li>This is an ordered list item</li>
    <li>It uses sequential numbers</li>
  </ol>

  {/* Quotes and Citations */}
  <blockquote>
    "This is a blockquote. It is used for longer quotes that are pulled out from
    the main body text."
    <cite>— Author Name</cite>
  </blockquote>

  <p>You can also use <q>inline quotes</q> for shorter phrases.</p>

  {/* Technical / Code Components */}
  <p>Use <code>code tags</code> for snippets, or the pre tag for blocks:</p>

  <pre>
    {`
      function helloWorld() {
        console.log("Hello!");
      }
    `}
  </pre>

  <p>Press <kbd>Ctrl</kbd> + <kbd>S</kbd> to save your progress.</p>

  {/* Miscellaneous */}
  <p>The formula for water is H<sub>2</sub>O, and E = mc<sup>2</sup>.</p>
  <p><del>This text was deleted</del> and <ins>this text was inserted</ins>.</p>
</Layout>
